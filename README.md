本项目用dev cpp写的，里面主要是链表的一些基本操作
比如创建一个链表，打印一个链表，统计链表的节点数，在链表的某处插入一个节点，
查看第index出的节点值，在链表的尾部插入节点，在链表的头部插入一个节点，
在链表的任意位置插入一个节点，删除一个链表，复制一个链表，把一个链表分为两段，
对链表进行排序，将两个按一定顺序排列的链表合并为一个，将两个链表相同的数合并为一个链表，
逆序反转链表.........